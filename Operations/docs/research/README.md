# Hub-level research notes

Investigation output from the `Explore` subagent or research-mode work that
spans multiple projects or informs a hub-level decision. Project-specific
research (e.g. SOPS's ERP/MRP benchmark) stays in that project's own
`docs/research/`. One file per investigation: `<slug>.md`.
