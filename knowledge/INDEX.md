# Knowledge Index

One line per file: topic, what it covers, last updated. Read this before
searching further — a matching row means the answer is already captured.

| File | Covers | Last updated |
|------|--------|---------------|
| `tlelosa-claude-config.md` | The private Claude Code plugin marketplace repo: structure, machine split (Operations/Pappa T), IT clearance status, CORE.md version, ADR-007/008/009/010; codex-gate install + network-off smoke-test confirmed; `/session-end` promotion and its first-run defects; the first real `hub-template/` reconcile (PR #14, **merged** 2026-08-07) showing ADR-008 drift runs both ways; **2026-08-08 systems check** — 16 branches stranded across two repos with no open PR, the mechanism behind it, what was recovered, and the `/continue` Step 1.8 + `/session-end` Step 1.5 checks that close it; **2026-08-09: the roster was authoritative in CORE.md and absent from disk on Pappa T** — six weeks of every delegation silently falling through to Claude Code built-ins with `Explore` inheriting `opus`, root-caused to the 2026-07-29 strip decision's accepted tradeoff, and fixed with a `SessionStart` hook + `bootstrap.mjs` + `roster-manifest.json` (CORE **1.5**, dcoe-roster **3.7.0**, `ab95eef`) rather than another written note; why Node not bash, why missing-only needs `--repair` to be safe, paranoid `settings.json` writes, codex-gate now unconditional on every machine by owner decision ahead of IT egress clearance, and the `gh` auth story — unauthenticated during that work (hence the direct-to-`main` merge), **fixed same day** via `gh auth login --web` as `tlelosa-web`, with the GCM-token shortcut blocked by the auto-mode classifier, a missing `workflow` scope that will reject `.github/workflows/` pushes at push time, and a first authenticated measurement of **11 unmerged branches with zero open PRs** across the two repos | 2026-08-09 |
| `nameplatetool.md` | NamePlateTool (Fan Movement work project): stack, frontend/backend/PDF payload contract, uvicorn-supervisor kill gotcha, open Excel-import bug (still unfixed, top cross-project priority), testing status | 2026-07-28 |
| `cratetracker.md` | CrateTracker PWA (F1 Clash premium-crate predictor): structure, deploy steps, crate-cycle model, state shape, win-vs-race-count semantics, service-worker update-prompt pattern (supersedes the bump-only cache-busting note) | 2026-08-06 |
| `pitwall-companion.md` | Pitwall Companion / F1 Clash Resource Sheet PWA: structure, localStorage schema, cache-busting (documented rule missed 3x in a row 2026-08-06, caught and fixed with a catch-up bump), the same stale-PWA two-open trap as CrateTracker with the update-prompt fix **not yet ported here**, modeling notes (boost scaling, live loadouts, Series-0 legendaries); repo moved `tlelosa-web`→`RMLRACE` 2026-08-06 | 2026-08-06 |
| `pitcrew-sync.md` | PitCrew Sync (3rd F1 Clash sibling PWA, `RMLRACE` org not `tlelosa-web`): spreadsheet-synced catalog + Ko-fi donation, PitWall-Companion-backup bootstrap converter, F1-red restyle after a visual-identity mismatch, `RMLRACE` push-access gotcha (2 commits may be sitting in an unapplied patch file, not yet on `origin/main`) | 2026-08-05 |
| `operations-hub.md` | Operations hub (Fan Movement work PC) cross-project findings: OneDrive+git corruption root cause and relocate-plus-junction fix; Operations ↔ cloud-environment git-sync bridge confirmed; live gitignored runtime-data inventory + backup procedure (sqlite backup API, data/secrets split); **current live path table as of 2026-08-09 — `C:\Dev\…` no longer exists on this machine and every cached Operations path predating the 2026-08-03 consolidation is suspect**, with the generalisable finding that a cached path is a claim with an expiry date nothing in its own text reveals (same shape as a stale session-log entry, applied to filesystem layout) — verifying one such table found a 33% error rate after two reviewer rounds; **2026-08-09: the Fan Movement contract was terminated 2026-08-03** — every other entry in that file was written while it was live and assumes it; company IP staged to `Desktop/Fan Movement - Company IP/` (917 files after pruning a swept-in Chrome profile, copy not move, DBs via sqlite backup API and verified), with the finding that "put it in a folder" is mostly not a filesystem task — the same material sits in five places and git history in three private repos is the larger footprint a folder structurally cannot touch, while a completed folder looks exactly like a completed job; daily backup task disabled so it stops copying the production DB to OneDrive, then **re-scoped and re-enabled the same day — and narrowing one scope revealed two leaks the other scope was already producing**: a signed-in Chrome profile syncing to OneDrive (credential stores escaped the patterns only by having no file extension — luck, not design) and a synced manifest listing the full path of every secret on the machine, with the reusable rules that a scope change is a discovery event, that "no X in the output" and "no reference to X" are different invariants, and that a new guard needs a positive control; **2026-08-10: both live vaults deleted, and the backup pointed at the missing one reported success while pruning real runs** — an absent input and an empty input are not the same thing but discovery-based tools return the identical empty list for both, so a retention policy must never act on a run that discovered nothing; `exists()` is not a sufficient guard where a plausible wrong path exists (this repo's `Pappa T/` snapshot holds no runtime state), and deleting a redundant copy silently promotes the remaining one to primary; **2026-08-10 (later): Pappa T restored to `~/Pappa T` and coverage resumed — and re-pointing `VAULTS` was the smallest part of it**: restore from the manifest's recorded source paths because the flattened filenames are not invertible, a config change in a script is not a config change in the scheduled task that runs it (the task still named the hub's own pre-move path, and would have died with a Python error rather than reaching the exit-4 guard), on Windows an `rmtree` that cannot fail is a bug (`ignore_errors=True` hid ReadOnly directories inherited by `copytree`, so four days of prunes deleted each run's files and left the shell, quietly costing one real retained run per cycle — third appearance of this attribute trap here), derive a display path from the vault rather than hardcoding its root or the drift report's excluded-vault label silently stops firing, and a file deliberately excluded from every copy has no copy (the gitignored, deliberately-unbacked-up Indeed Chrome profile) | 2026-08-10 |
| `pappa-t.md` | Pappa T (personal machine): what it is vs. Operations, no live remote-environment bridge (git sync only), Pappa T-only items (codex-gate), 2026-07-28 vault survey findings; stale duplicate hub clone removed 2026-08-06 (corrects that survey's gitlink call); vault now pushes to private `tlelosa-web/pappa-t` on `main`, with the pre-push secrets/personal-data audit that cleared it | 2026-08-06 |
| `tebelloreborn.md` | TebelloReborn (Career Engine, Pappa T sub-project): job-application automation pipeline — stack, 5-stage MVP, ADR-003 inference split, doc-gen prompt-injection fix, fpdf2/Apify gotchas; Stage 6 submission core built 2026-08-06; Indeed adapter spec written + Codex-reviewed 2026-08-07 (platform/ToS/dependency decisions resolved directly with Tebello, reCAPTCHA + open-ended screening-question findings from live `claude-in-chrome` recon reshaping scope, a same-morning concurrent-session collision reconciled); Codex fold-in completed same day, spec now build-ready — carries two runtime-failure traps found by reading code the spec described from memory ("just Python validation" is only true when the column already exists; a CHECK inlined in `CREATE TABLE IF NOT EXISTS` has a silent expiry), plus why a "cheap" capability predicate wasn't. **Stage 6 Phases A, B, C, D built 2026-08-07, E–H remaining** (538 tests, offline throughout, adapter registry still empty) — ADR-004 replaced the shared `PRAGMA user_version` counter with a per-module ledger, unblocking Phase B's table rebuild that no column-existence check could have gated; Phase B's `submission_preps` exists because zero question rows can't distinguish "never prepped" from "no questions"; Phase C's two orderings (gate before session check, `--all` refusal last) each have a test because both fail silently; **Phase D's `browser.py` shows how to build and fully test a module that will import `playwright` without having it** — split judgment from observation so the adapter observes the page and the module judges what was observed, never querying a DOM, plus three spec gaps closed by refusing to invent what recon never verified (landmark selectors, the review step's URL segment, extra auth markers). **Phase E's offline half built 2026-08-08 (676 tests)** and the live half stopped by the real site: Indeed refuses an automated sign-in (fixed by moving the login out of automation into a dedicated hand-signed Chrome profile, never by defeating detection), a bounding box is not visibility so the reCAPTCHA v3 badge aborts every healthy run unless the observer uses a real `is_visible()` — with the judgment/observation split proving its worth by localising that bug to one function — wait on rendered content and never the URL (the wizard renders ~10–12s after the URL is already right), and Cloudflare bot-challenges after ~4 automated runs in 15 minutes, which makes pacing a design constraint and is evidence about Phase G rather than only Phase E | 2026-08-08 |
| `hub-process.md` | How this hub's own rules behave in practice: hub-and-spoke reconciliation direction ("the project file wins" is about ownership, not correctness — bring the authoritative file current, then trim the hub to a pointer; state gates in full in both files), why contention-file sync must be re-checked in the same command as the commit when another session is live, and why "is the final log entry accurate?" must be answered by comparing commit clocks across hub + live sub-project rather than by reading the entry — which recurred a third time, showing the check belongs in `/continue`'s orient step (the reading session is the one that can be wrong) and not in `/session-end`, which asks at the only moment guaranteed to answer "fine"; that check is now **installed** as `/continue` Step 1.9 — with the finding that a `knowledge/` entry is a record and not a control (three write-ups did not stop three recurrences; only the command file changes what runs), that the prescribed "Step 1" placement was wrong because Step 1.75 is what pulls `origin/main`, that a *passing* check must still be reported since silence and never-ran look identical, and why the ADR-008 fold-up into `hub-template/` is blocked on the template being four improvements behind rather than on permission ; **2026-08-08 stranded-branch failure mode** — pushed is not delivered, why four weeks of it left no symptom, the two-check fix (`/session-end` Step 1.5 outbound, `/continue` Step 1.8 inbound), and two measurement traps: diff branches against `origin/main` not the merge-base (a 68-commit vault re-merge made merge-base diffs look like 43k lines of loss when 7 of 13 branches held nothing), and unmerged-by-ancestry is not lost content; **2026-08-09 — what the first actual recovery taught**, which the detectors don't cover: never merge a stranded branch to recover it (this one diffed as 16,050 deletions that were all `main`'s own later work — extract the few real files with `git archive` and delete the branch instead), "everything but N files is stale" is a summary and not a finding (diffing the other 79 file-by-file surfaced one real line `main` never had — a provenance link from a command back to the spec it implements), and deletion is the last step, after an owner call *and* a file-level accounting of what is being discarded; **2026-08-12 — recovering log *entries* (not files) from a stranded branch**: a branch's own "previous entry" is not evidence of where it belongs in `main` (concurrent branches forked from the same point and never saw each other's later merges) — instead walk `git log`+`git show` on `main` to timestamp when each existing entry was actually added, cross-reference each orphan's own commit timestamp against that timeline, and verify the splice numerically (em-dash count, header count, no backward date transitions) rather than by re-reading; also noting `tlelosa-web/Claude-Code`'s `main` has a PR-required branch-protection rule that a direct push currently bypasses rather than being blocked by | 2026-08-12 |
| `session-tooling.md` | **2026-08-09: a `CHECKSUMS.txt` written by Python on Windows fails `sha256sum -c` on every line** — text mode writes CRLF, `sha256sum` takes the rest of the line as the filename so all entries fail at once and it reads like corrupt data rather than a bad manifest; fix is `newline=""`, and the reusable part is that a verification artifact is only verified by the tool it tells someone else to use (a Python spot-check passed the same broken file because `splitlines()` strips `\r`). Plus the earlier shell/tool gotchas, project-independent: `git commit -m` with a PowerShell here-string splits into pathspecs (use `-F <file>`), and appending to docs files needs Python rather than `Add-Content` for encoding safety — generalised 2026-08-08 to **any** PowerShell round-trip of a source file, since `Get-Content -Raw` decodes a BOM-less UTF-8 file as ANSI and silently mojibakes every em-dash while tests still pass; plus Playwright's `on()` rejecting a builtin method as an event handler; **2026-08-10: PowerShell's `..` range operator silently mangles a git `A..B` revision range built with a variable** — returned `0` where the true answer was `625`, i.e. succeeded with a wrong, plausible value, so quote the range or use `git merge-base --is-ancestor`; the recurring shape across three entries here is a shell rewriting an argument before the tool sees it | 2026-08-10 |
| `claude-code-sessions.md` | Claude Code internals: `~/.claude/projects/` path/encoding scheme (unrelated to claude.ai Projects), transcripts-key-on-cwd vs. auto-memory-keys-on-repo split, `/cd`/`--add-dir`/`--continue`/`/branch`/`/rewind` file behavior, plaintext-at-rest security note, `cleanupPeriodDays` vs. never-swept `history.jsonl`, Cowork remote/local storage split | 2026-07-30 |
| `cloud-sessions.md` | Claude Code remote/cloud sessions: agent-proxy host blocking (HTTP 000 ≠ empty page) and how to verify deploys without reaching the live URL, oversized `mcp__github__actions_list` output, Playwright global-install/ESM/service-worker testing setup | 2026-08-06 |
| `ai-outreach-agency.md` | ai-outreach-agency (Pappa T sub-project): B2B outreach pipeline — stack, 6-stage pipeline, OpenRouter-credits + Ollama-timeout open items, ADR-004, db_path/approvals wiring bugs | 2026-07-28 |
| `mims-app.md` | MIMS App (Pappa T sub-project): MRP web app — Next.js/Supabase/Tailwind stack, Gemini-driven (not Claude), Shop Floor stage in progress | 2026-07-28 |
| `iq-signal-generator.md` | IQ Option Signal Generator (Pappa T sub-project): regime-filtered trading-signal CLI — ADX/RSI/Stochastic logic, risk-management stops | 2026-07-28 |
| `tenders-sa.md` | Tenders (Pappa T sub-project): SA tender-monitoring automation — structure, `tenders-sa` submodule status, dormant bid-package folder note | 2026-07-28 |
| `sops.md` | SOPS (Fan Movement work project): Flask/SQLAlchemy/SQLite stack, TDD + held-migration convention, stale-dev-server gotcha, git-worktree/OneDrive history, concurrent-session git contamination pattern, AvgMovement-migration-go-ahead + Payment-Status-review outstanding items | 2026-07-28 |
| `delivery-note-system.md` | Delivery Note (Fan Movement work project): Next.js 16/Prisma 7/SQLite stack, Prisma-7 driver-adapter gotcha, Turbopack+Windows junction workaround, DN-number generation fix, no-test-suite outstanding item | 2026-07-28 |
| `daily-sales-order-files.md` | Daily Sales Order Files (Fan Movement work pipeline): 5-stage script pipeline, load-bearing folder layout, external OneDrive dependencies, self-correcting filename resolution, blank-over-stale design decision, healthy/no outstanding items | 2026-07-28 |
