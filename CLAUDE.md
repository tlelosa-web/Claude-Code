# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Current state

This repository is currently a scaffold: it contains only `README.md` (project title `Claude-Code`). There is no source code, build system, dependency manifest, or test suite yet, so there are no build/lint/test commands to document at this time.

## Guidance for future work

When code is first added to this repository, update this file to capture:

- **Commands** — how to build, run, lint, and test the project, including how to run a single test.
- **Architecture** — the big-picture structure that spans multiple files and isn't obvious from a quick directory scan.

Add these details once the corresponding tooling and structure actually exist; do not document commands or architecture that have not been established.
