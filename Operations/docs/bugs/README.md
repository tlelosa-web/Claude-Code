# Hub-level bug reports

Most bugs belong in the project they occur in (e.g. `2. SOPS/docs/bugs/`).
Only use this folder for issues that span multiple projects or the hub
setup itself. One file per bug: `<slug>.md`, written by the `debugger`
agent with root cause before an `executor` touches anything.
