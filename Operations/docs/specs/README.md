# Hub-level specs

Feature/setup specs for work done at root or spanning multiple projects,
written by the `planner`/Context agent before any `executor` builds
anything. Project-specific specs stay in that project's own `docs/specs/`.
One file per spec: `<feature>.md`.
