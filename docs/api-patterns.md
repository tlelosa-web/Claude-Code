# API Patterns

No vault-wide API contract is currently defined.

For app-specific work, inspect the target project first:

- `MIMS App/` appears to be a Next.js/Supabase application.
- `Tenders/` appears to be a Python automation project.
- `IQ/` appears to be a Python signal-generation project.

Do not assume a shared API pattern across projects.
